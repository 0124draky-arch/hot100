# LeetCode Hot 100 知识笔记

> 本文档记录刷题过程中的核心知识点、代码模板、解题思路
> 最后更新：2026-06-16

---

## 📋 目录
- [第一部分：核心思路模板](#第一部分核心思路模板)
- [第二部分：基础知识讲解](#第二部分基础知识讲解)
- [第三部分：题目详解](#第三部分题目详解)

---

# 第一部分：核心思路模板

## 1. 哈希表模式

### 1.1 两数之和模式（查找配对）
**适用场景：** 需要找两个元素满足某种关系（和、差、积等）

**核心思路：** 边遍历边查找，用哈希表存储"已经遍历过的元素"

```javascript
function twoSum(nums, target) {
    const map = new Map(); // 存储 值 -> 索引
    for (let i = 0; i < nums.length; i++) {
        const complement = target - nums[i]; // 需要找的另一个数
        if (map.has(complement)) {
            return [map.get(complement), i];
        }
        map.set(nums[i], i); // 把当前数存入
    }
}
```

**时间复杂度：** O(n)  
**空间复杂度：** O(n)

---

### 1.2 计数/频率统计模式
**适用场景：** 统计元素出现次数、分组

```javascript
function countFrequency(arr) {
    const map = new Map();
    for (const item of arr) {
        map.set(item, (map.get(item) || 0) + 1);
    }
    return map;
}
```

---

### 1.3 Set 起点判断模式（最长连续序列）
**适用场景：** 判断元素是否存在、去重、连续序列问题

**核心思路：** 用 Set 快速判断数字是否存在，只从“真正的起点”开始统计，避免重复计算。

```javascript
function longestConsecutive(nums) {
    const set = new Set(nums);
    let maxLen = 0;

    for (const num of set) {
        if (!set.has(num - 1)) {
            let current = num;
            let len = 0;

            while (set.has(current)) {
                current++;
                len++;
            }

            maxLen = Math.max(maxLen, len);
        }
    }

    return maxLen;
}
```

**关键点：**
- `Set` 只关心元素在不在，不需要存键值对
- `!set.has(num - 1)` 表示 `num` 是一段连续序列的起点
- 只从起点往后数，每段序列只统计一次

**时间复杂度：** O(n)  
**空间复杂度：** O(n)

---

### 1.4 前缀和 + 哈希表
**适用场景：** 子数组和问题、连续子数组问题

**核心思路：** 前缀和 + 哈希表存储"前缀和出现的次数"

```javascript
function subarraySum(nums, k) {
    const map = new Map();
    map.set(0, 1);

    let sum = 0;
    let count = 0;

    for (const num of nums) {
        sum += num;
        if (map.has(sum - k)) {
            count += map.get(sum - k);
        }
        map.set(sum, (map.get(sum) || 0) + 1);
    }

    return count;
}
```

**关键点：**
- `sum - k` 的含义：如果存在这个前缀和，那么中间部分的和就是 k
- 初始化 `map.set(0, 1)` 处理从头开始的子数组

---

## 2. 数组模式

### 2.1 前缀积 + 后缀滚动乘积
**适用场景：** 需要计算“除当前位置以外”的乘积，且不能使用除法

**核心思路：** `answer` 先存每个位置左边的乘积，再从右往左用变量 `right` 滚动维护右边乘积。

```javascript
function productExceptSelf(nums) {
    const n = nums.length;
    const answer = new Array(n);

    answer[0] = 1;
    for (let i = 1; i < n; i++) {
        answer[i] = answer[i - 1] * nums[i - 1];
    }

    let right = 1;
    for (let i = n - 1; i >= 0; i--) {
        answer[i] *= right;
        right *= nums[i];
    }

    return answer;
}
```

**关键点：**
- 第一遍结束后，`answer[i]` 表示 `nums[i]` 左边所有数的乘积
- 第二遍处理 `i` 前，`right` 表示 `nums[i]` 右边所有数的乘积
- 必须先 `answer[i] *= right`，再 `right *= nums[i]`，否则会把当前位置自己算进去

**时间复杂度：** O(n)  
**额外空间复杂度：** O(1)，输出数组 `answer` 不计入额外空间

### 2.2 数组中的快慢指针找环
**适用场景：** 数组长度为 `n + 1`，数字范围为 `1 ~ n`，要求不修改数组并用 O(1) 额外空间找重复数。

**核心思路：** 把数组看成“下标指向 `nums[i]` 的隐式链表”，重复数字就是环入口。

```javascript
function findDuplicate(nums) {
    let slow = 0;
    let fast = 0;

    do {
        slow = nums[slow];
        fast = nums[nums[fast]];
    } while (slow !== fast);

    let ptr1 = 0;
    let ptr2 = slow;

    while (ptr1 !== ptr2) {
        ptr1 = nums[ptr1];
        ptr2 = nums[ptr2];
    }

    return ptr1;
}
```

**关键点：**
- `nums[i]` 表示从下标 `i` 下一步跳到哪个下标
- 数字范围是 `1 ~ n`，所以不会跳回下标 `0`，`0` 只是进入链表的起点
- `fast = nums[nums[fast]]` 表示快指针连续跳两步
- 第一阶段相遇后，`slow` 和 `fast` 都可以作为相遇点
- 第二阶段一个指针从 `0` 出发，另一个从相遇点出发，同速走，再次相遇就是环入口

**时间复杂度：** O(n)  
**空间复杂度：** O(1)

---

### 287. 寻找重复数
**难度：** 中等 ⭐⭐⭐  
**完成日期：** 2026-06-15

**题目：** 给定一个包含 `n + 1` 个整数的数组 `nums`，其中每个整数都在 `1 ~ n` 之间，找出唯一重复的数字。要求不能修改数组，并且只能使用 O(1) 的额外空间。

**核心思路：**
- 把数组看成“下标指向 `nums[i]` 的隐式链表”
- 因为值域是 `1 ~ n`，所以从 `0` 出发一定会进入 `1 ~ n` 中的某个环
- 重复数字对应的就是环入口
- 第一阶段用快慢指针在环里相遇，第二阶段一个指针回到起点，另一个留在相遇点，同速前进，最终相遇点就是环入口

**代码：**
```javascript
var findDuplicate = function(nums) {
    let slow = 0;
    let fast = 0;

    do {
        slow = nums[slow];
        fast = nums[nums[fast]];
    } while (slow !== fast);

    let ptr1 = 0;
    let ptr2 = slow;

    while (ptr1 !== ptr2) {
        ptr1 = nums[ptr1];
        ptr2 = nums[ptr2];
    }

    return ptr1;
};
```

**时间复杂度：** O(n)  
**空间复杂度：** O(1)

**易错点：**
- `while (slow !== fast)` 不能直接用在第一阶段，因为初始时两者都等于 0，循环不会执行
- `fast = nums[nums[fast]]` 才表示快指针走两步
- 第二阶段里 `ptr1` 和 `ptr2` 每次都只能走一步，不能再让快指针走两步
- `ptr2` 可以写成 `slow` 或 `fast`，因为第一阶段结束时它们已经相遇

**学习心得：**
- 这个题最难的不是代码，而是把数组理解成“传送门”模型
- `nums[i]` 不是普通的数值，而是“下一步要跳到的下标”
- 重复数字之所以是环入口，是因为有两个不同下标指向同一个下标
- 用 `Set` 也能找重复数，但额外空间是 O(n)，不满足题目要求

---

## 3. 双指针模式

### 2.1 对撞指针（两端向中间）
**适用场景：** 有序数组、需要从两端查找

```javascript
function twoPointers(arr) {
    let left = 0, right = arr.length - 1;
    while (left < right) {
        if (满足条件) {
            // 处理逻辑
            left++;
            right--;
        } else if (需要增大) {
            left++;
        } else {
            right--;
        }
    }
}
```

---

### 2.2 快慢指针
**适用场景：** 链表找中点、判断环、数组原地修改

```javascript
// 数组原地修改
function fastSlowPointers(arr) {
    let slow = 0;
    for (let fast = 0; fast < arr.length; fast++) {
        if (满足条件) {
            arr[slow] = arr[fast];
            slow++;
        }
    }
    return slow;
}
```

---

### 2.3 滑动窗口（重要！）
**适用场景：** 子串/子数组问题、连续区间问题

**核心思路：** 维护一个动态窗口，右指针扩展，左指针收缩

```javascript
function slidingWindow(s) {
    const map = new Map(); // 窗口内元素统计
    let left = 0, right = 0;
    let result = 初始值;
    
    while (right < s.length) {
        // 1. 扩大窗口：right指针移动，加入新元素
        const c = s[right];
        right++;
        map.set(c, (map.get(c) || 0) + 1);
        
        // 2. 判断是否需要收缩窗口
        while (窗口需要收缩的条件) {
            // 3. 缩小窗口：left指针移动，移除元素
            const d = s[left];
            left++;
            map.set(d, map.get(d) - 1);
        }
        
        // 4. 更新结果
        result = Math.max(result, right - left);
    }
    return result;
}
```

**滑动窗口四步法：**
1. 右指针扩展：加入新元素
2. 判断收缩：窗口不满足条件时收缩
3. 左指针收缩：移除左边元素
4. 更新答案：在合适的时机更新结果

**一句话模板：** 右指针负责探索新元素，左指针负责修复窗口；窗口不合法时移动左指针，合法时更新答案。

#### 无重复字符窗口
**适用场景：** 找最长不含重复字符的连续子串。

**Set 写法：**
```javascript
function lengthOfLongestSubstring(s) {
    const set = new Set();
    let left = 0;
    let maxLen = 0;

    for (let right = 0; right < s.length; right++) {
        while (set.has(s[right])) {
            set.delete(s[left]);
            left++;
        }

        set.add(s[right]);
        maxLen = Math.max(maxLen, right - left + 1);
    }

    return maxLen;
}
```

**Map 跳跃写法：**
```javascript
function lengthOfLongestSubstring(s) {
    const map = new Map();
    let left = 0;
    let maxLen = 0;

    for (let right = 0; right < s.length; right++) {
        const char = s[right];

        if (map.has(char) && map.get(char) >= left) {
            left = map.get(char) + 1;
        }

        map.set(char, right);
        maxLen = Math.max(maxLen, right - left + 1);
    }

    return maxLen;
}
```

**关键点：**
- Set 写法遇到重复字符时，要用 `while` 一直删到窗口里没有当前字符为止
- Map 写法里 `map.get(char) >= left` 不能省，因为 Map 里记录的是历史位置，旧字符可能已经不在当前窗口里
- `left` 只能向右移动，不能回头

---

## 3. 链表模式

### 3.1 虚拟头节点（哑节点）
**适用场景：** 需要修改链表头节点

```javascript
function processLinkedList(head) {
    const dummy = new ListNode(0);
    dummy.next = head;
    let curr = dummy;
    
    while (curr.next) {
        // 处理逻辑
        curr = curr.next;
    }
    
    return dummy.next;
}
```

---

### 3.2 快慢指针找中点
```javascript
function findMiddle(head) {
    let slow = head, fast = head;
    while (fast && fast.next) {
        slow = slow.next;
        fast = fast.next.next;
    }
    return slow;
}
```

---

### 3.3 反转链表（迭代）
```javascript
function reverseList(head) {
    let prev = null, curr = head;
    while (curr) {
        const next = curr.next; // 先保存下一个节点
        curr.next = prev;       // 反转指针
        prev = curr;            // prev前进
        curr = next;            // curr前进
    }
    return prev;
}
```

---

## 4. 栈与队列模式

### 4.1 括号匹配
```javascript
function isValid(s) {
    const stack = [];
    const map = { ')': '(', '}': '{', ']': '[' };
    
    for (const char of s) {
        if (char in map) {
            if (stack.pop() !== map[char]) return false;
        } else {
            stack.push(char);
        }
    }
    return stack.length === 0;
}
```

---

### 4.2 单调栈（下一个更大元素）
**适用场景：** 找"下一个更大/更小元素"

```javascript
function nextGreaterElement(nums) {
    const result = new Array(nums.length).fill(-1);
    const stack = []; // 存储索引
    
    for (let i = 0; i < nums.length; i++) {
        while (stack.length && nums[i] > nums[stack[stack.length - 1]]) {
            const idx = stack.pop();
            result[idx] = nums[i];
        }
        stack.push(i);
    }
    return result;
}
```

**关键点：**
- 栈内存储索引而不是值
- 维护单调递减栈找"下一个更大"
- 维护单调递增栈找"下一个更小"

---

## 5. 二叉树模式

### 5.1 递归遍历
```javascript
// 前序：根 -> 左 -> 右
function preorder(root) {
    if (!root) return;
    处理当前节点(root);
    preorder(root.left);
    preorder(root.right);
}

// 中序：左 -> 根 -> 右（BST中序是有序的！）
function inorder(root) {
    if (!root) return;
    inorder(root.left);
    处理当前节点(root);
    inorder(root.right);
}

// 后序：左 -> 右 -> 根（适合自底向上）
function postorder(root) {
    if (!root) return;
    postorder(root.left);
    postorder(root.right);
    处理当前节点(root);
}
```

---

### 5.2 层序遍历（BFS）
```javascript
function levelOrder(root) {
    if (!root) return [];
    const result = [];
    const queue = [root];
    
    while (queue.length) {
        const levelSize = queue.length;
        const currentLevel = [];
        
        for (let i = 0; i < levelSize; i++) {
            const node = queue.shift();
            currentLevel.push(node.val);
            if (node.left) queue.push(node.left);
            if (node.right) queue.push(node.right);
        }
        result.push(currentLevel);
    }
    return result;
}
```

---

## 6. 动态规划模式

### 6.1 一维DP
```javascript
function climbStairs(n) {
    if (n <= 2) return n;
    const dp = new Array(n + 1);
    dp[1] = 1;
    dp[2] = 2;
    for (let i = 3; i <= n; i++) {
        dp[i] = dp[i-1] + dp[i-2];
    }
    return dp[n];
}
```

---

### 6.2 二维DP
```javascript
function uniquePaths(m, n) {
    const dp = Array(m).fill(0).map(() => Array(n).fill(0));
    for (let i = 0; i < m; i++) dp[i][0] = 1;
    for (let j = 0; j < n; j++) dp[0][j] = 1;
    
    for (let i = 1; i < m; i++) {
        for (let j = 1; j < n; j++) {
            dp[i][j] = dp[i-1][j] + dp[i][j-1];
        }
    }
    return dp[m-1][n-1];
}
```

---

# 第二部分：基础知识讲解

## JavaScript Map（哈希表）

### 创建和基本操作
```javascript
// 创建
const map = new Map();

// 添加/更新
map.set(key, value);

// 获取
map.get(key);  // 返回 value，不存在返回 undefined

// 检查是否存在（重要！）
map.has(key);  // 返回 true/false

// 删除
map.delete(key);

// 获取大小
map.size;

// 清空
map.clear();
```

### 遍历
```javascript
// 方式1：for...of 遍历键值对
for (const [key, value] of map) {
    console.log(key, value);
}

// 方式2：遍历键
for (const key of map.keys()) {
    console.log(key);
}

// 方式3：遍历值
for (const value of map.values()) {
    console.log(value);
}
```

### 常用技巧
```javascript
// 计数：如果key存在则+1，不存在则从0开始
map.set(key, (map.get(key) || 0) + 1);

// 初始化时设置默认值
const map = new Map([[0, 1], [1, 2]]);
```

### 易错点
```javascript
// ❌ 错误：直接+1会得到NaN
map.set(key, map.get(key) + 1);  // undefined + 1 = NaN

// ✅ 正确：先判断是否存在
map.set(key, (map.get(key) || 0) + 1);
```

---

## JavaScript Set（哈希集合）

### 基本操作
```javascript
// 创建
const set = new Set();

// 添加
set.add(value);

// 检查是否存在
set.has(value);  // 返回 true/false

// 删除
set.delete(value);

// 获取大小
set.size;

// 清空
set.clear();

// 从数组创建（自动去重）
const set = new Set([1, 2, 2, 3]);  // Set {1, 2, 3}
```

### 遍历
```javascript
for (const value of set) {
    console.log(value);
}
```

### 常用技巧
```javascript
// 数组去重
const arr = [1, 2, 2, 3];
const uniqueArr = [...new Set(arr)];  // [1, 2, 3]

// Set 转数组
const arr = Array.from(set);
```

---

## JavaScript 数组常用方法

### 添加/删除元素
```javascript
// 末尾添加
arr.push(item);        // 返回新长度

// 末尾删除
arr.pop();             // 返回删除的元素

// 开头添加
arr.unshift(item);     // 返回新长度

// 开头删除
arr.shift();           // 返回删除的元素
```

### 数组切片和拼接
```javascript
// 切片（不改变原数组）
arr.slice(start, end);  // 返回 [start, end) 的新数组

// 拼接/删除/插入（改变原数组）
arr.splice(start, deleteCount, item1, item2, ...);

// 反转数组（改变原数组）
arr.reverse();
```

### reverse 方法
```javascript
const row = [1, 4, 7];
row.reverse();
console.log(row); // [7, 4, 1]
```

**关键点：**
- `reverse()` 是 JavaScript 数组的内置方法
- 它会直接修改原数组，不会创建一个新的反转数组
- 返回值也是反转后的原数组本身
- 在矩阵题里，`matrix[i].reverse()` 表示把第 `i` 行原地反转

### 数组排序
```javascript
// 默认按字符串排序
arr.sort();

// 数字排序
arr.sort((a, b) => a - b);  // 升序
arr.sort((a, b) => b - a);  // 降序
```

### 数组遍历
```javascript
// for 循环
for (let i = 0; i < arr.length; i++) {
    console.log(arr[i]);
}

// for...of（推荐）
for (const item of arr) {
    console.log(item);
}

// forEach
arr.forEach((item, index) => {
    console.log(item, index);
});
```

### 二维数组/矩阵遍历
```javascript
const matrix = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

const n = matrix.length;

// 遍历每一个位置 matrix[i][j]
for (let i = 0; i < n; i++) {
    for (let j = 0; j < n; j++) {
        console.log(matrix[i][j]);
    }
}
```

**关键点：**
- `matrix[i]` 表示第 `i` 行
- `matrix[i][j]` 表示第 `i` 行第 `j` 列
- 外层循环通常控制行 `i`，内层循环通常控制列 `j`

### 矩阵转置
```javascript
// 沿主对角线交换：matrix[i][j] <-> matrix[j][i]
for (let i = 0; i < n; i++) {
    for (let j = i + 1; j < n; j++) {
        const temp = matrix[i][j];
        matrix[i][j] = matrix[j][i];
        matrix[j][i] = temp;
    }
}
```

**关键点：**
- 主对角线上的元素 `matrix[i][i]` 不需要交换
- 只遍历主对角线右上方区域，也就是 `j = i + 1`
- 如果整个矩阵都遍历，会把同一对元素交换两次，最后等于没变

---

## JavaScript let 和 const

### 核心区别
```javascript
let count = 0;
count++; // 可以，let 定义的变量可以重新赋值

const target = 10;
target = 20; // 错误，const 定义的变量不能重新赋值
```

### const 不代表内容不能变
```javascript
const arr = [1, 2, 3];
arr.push(4); // 可以，arr 仍然指向原来的数组

const set = new Set();
set.add(1); // 可以，set 仍然指向原来的 Set

const map = new Map();
map.set(1, 2); // 可以，map 仍然指向原来的 Map
```

**关键点：**
- `let`：变量后面需要重新赋值时使用
- `const`：变量名不会重新指向其他值时使用
- `const` 限制的是变量绑定，不限制数组、对象、Set、Map 内部内容变化
- 算法题中，能用 `const` 就先用 `const`；发现变量本身要变，再改成 `let`

---

## JavaScript 字符串常用方法

### 基本操作
```javascript
// 获取长度
str.length;

// 获取字符
str[i];           // 或 str.charAt(i)

// 获取字符编码
str.charCodeAt(i);

// 从字符编码创建字符
String.fromCharCode(65);  // 'A'
```

### 字符串切割和拼接
```javascript
// 切割成数组
str.split('');      // 按字符切割
str.split(' ');     // 按空格切割

// 数组拼接成字符串
arr.join('');       // 无分隔符
arr.join(' ');      // 空格分隔
```

### 字符串查找
```javascript
// 查找子串位置
str.indexOf('abc');      // 返回索引，找不到返回-1
str.includes('abc');     // 返回 true/false
```

### 字符串截取
```javascript
// 截取子串
str.substring(start, end);  // [start, end)
str.slice(start, end);      // [start, end)，支持负数
```

---

# 第三部分：题目详解

## 哈希表专题

### 1. 两数之和
**难度：** 简单 ⭐⭐⭐  
**完成日期：** 2026-05-25

**题目：** 给定数组和目标值，找出两个数的索引使其和为目标值。

**核心思路：**
- 边遍历边查找，用哈希表存储"已经遍历过的值和索引"
- 对于当前元素，计算 `complement = target - nums[i]`
- 在哈希表中查找 complement 是否存在

**代码：**
```javascript
var twoSum = function(nums, target) {
    const map = new Map();
    for(let i = 0; i < nums.length; i++) {
        const complement = target - nums[i];
        if(map.has(complement)) {
            return [map.get(complement), i];
        }
        map.set(nums[i], i);
    }
};
```

**时间复杂度：** O(n)  
**空间复杂度：** O(n)

**易错点：**
- ❌ 返回值顺序：应该是 `[map.get(complement), i]` 而不是 `[i, map.get(complement)]`，**先返回已经存了的**
- ❌ 先全部存入再查找 vs 边遍历边查找：两种都对，但后者更简洁

**学习心得：**
- 两种方案对比：
  - 方案A：先全部存入map，再遍历查找 - 需要两次遍历，需要判断 `!== i`
  - 方案B：边遍历边查找 - 只需一次遍历，代码更简洁
  - 结论：两种都正确，主要区别在效率和代码简洁性

---

### 128. 最长连续序列
**难度：** 中等 ⭐⭐⭐  
**完成日期：** 2026-06-10

**题目：** 给定一个未排序的整数数组，找出数字连续的最长序列长度，要求时间复杂度为 O(n)。

**核心思路：**

- 先用 `Set` 去重，并支持 O(1) 判断元素是否存在
- 遍历每个数字，只从 `num - 1` 不存在的数字开始统计
- 如果 `num - 1` 存在，说明 `num` 不是起点，直接跳过
- 从起点开始不断判断 `current` 是否存在，统计连续长度

**代码：**

```javascript
var longestConsecutive = function(nums) {
    const set = new Set(nums);
    let maxLen = 0;

    for (const num of set) {
        if (!set.has(num - 1)) {
            let current = num;
            let len = 0;

            while (set.has(current)) {
                current++;
                len++;
            }

            maxLen = Math.max(maxLen, len);
        }
    }

    return maxLen;
};
```

**时间复杂度：** O(n)  
**空间复杂度：** O(n)

**易错点：**
- 如果写 `let len = 1`，while 条件应写 `set.has(current + 1)`，否则会重复计算起点
- 如果写 `while (set.has(current))`，`len` 应该从 0 开始
- 不能用普通排序满足题目要求，因为排序通常是 O(n log n)

**学习心得：**
- `Map` 是键值对，适合存“值 -> 信息”；`Set` 是集合，**适合判断“元素在不在”**
- 判断起点的标准是：前一个数字 `num - 1` 不存在
- 虽然代码有 `for + while`，但每段连续序列只从起点统计一次，所以整体仍是 O(n)

---

### 560. 和为 K 的子数组
**难度：** 中等 ⭐⭐⭐  
**完成日期：** 2026-06-11

**题目：** 给定一个整数数组和整数 `k`，统计和为 `k` 的连续子数组个数。

**核心思路：**
- `sum` 表示从数组开头累加到当前位置的前缀和
- `map` 存的是“历史前缀和 -> 出现次数”
- 如果当前前缀和是 `sum`，目标子数组和是 `k`，就要找之前是否出现过 `sum - k`
- 因为 `sum - oldSum = k`，所以 `oldSum = sum - k`
- 每一轮必须先查历史前缀和，再把当前 `sum` 存入 `map`

**代码：**
```javascript
var subarraySum = function(nums, k) {
    const map = new Map();
    map.set(0, 1);

    let sum = 0;
    let count = 0;

    for (const num of nums) {
        sum += num;

        if (map.has(sum - k)) {
            count += map.get(sum - k);
        }

        map.set(sum, (map.get(sum) || 0) + 1);
    }

    return count;
};
```

**时间复杂度：** O(n)  
**空间复杂度：** O(n)

**易错点：**
- `map` 存的不是数组元素出现次数，而是前缀和出现次数
- 初始化 `map.set(0, 1)` 是为了统计从下标 0 开始的子数组
- 必须先查 `sum - k`，再存当前 `sum`，否则 `k = 0` 时可能多算空子数组
- 空间复杂度是 O(n)，因为最多会记录 n 个不同的前缀和，使用的是 `Map` 不是 `Set`

**学习心得：**
- `map.get(sum - k)` 的值表示有几个历史位置能作为子数组起点前面的位置
- 同一个前缀和可能出现多次，所以要存次数，而不是只存是否存在
- 一句话核心思路：用 `map` 记录历史前缀和次数，当前 `sum` 每次寻找历史里的 `sum - k`

---

## 数组专题

### 238. 除自身以外数组的乘积
**难度：** 中等 ⭐⭐⭐  
**完成日期：** 2026-06-14

**题目：** 给定整数数组 `nums`，返回数组 `answer`，其中 `answer[i]` 等于 `nums` 中除 `nums[i]` 之外其余元素的乘积。要求不使用除法，并在 O(n) 时间内完成。

**核心思路：**
- 每个位置的答案 = 左边所有数的乘积 × 右边所有数的乘积
- 第一遍从左到右，让 `answer[i]` 先保存 `nums[i]` 左边所有数的乘积
- 第二遍从右到左，用变量 `right` 滚动保存**当前位置右边所有数的乘积**
- 处理当前位置时，先把 `answer[i]` 乘上 `right`，再把 `nums[i]` 乘进 `right`

**代码：**
```javascript
var productExceptSelf = function(nums) {
    const n = nums.length;
    const answer = new Array(n);

    answer[0] = 1;
    for (let i = 1; i < n; i++) {
        answer[i] = answer[i - 1] * nums[i - 1];
    }

    let right = 1;
    for (let i = n - 1; i >= 0; i--) {
        answer[i] *= right;
        right *= nums[i];
    }

    return answer;
};
```

**时间复杂度：** O(n)  
**额外空间复杂度：** O(1)，输出数组 `answer` 不计入额外空间

**易错点：**
- `right` 会不断更新，所以必须用 `let`，不能用 `const`
- 第二遍必须先 `answer[i] *= right`，再 `right *= nums[i]`，否则会把当前位置自己算进去
- `answer[i]` 表示修改某个位置，不能写成 `answer *= right`

**学习心得：**
- 第一遍结束后，`answer` 暂时不是最终答案，而是左乘积数组
- 第二遍中，处理 `i` 前的 `right` 表示 `nums[i]` 右边所有数的乘积
- 优化版把输出数组复用成左乘积数组，只额外使用一个 `right` 变量，因此额外空间是 O(1)

---

### 287. 寻找重复数
**难度：** 中等 ⭐⭐⭐  
**完成日期：** 2026-06-15

**题目：** 给定一个包含 `n + 1` 个整数的数组 `nums`，其中每个整数都在 `1 ~ n` 范围内，找出重复的数字。要求不能修改数组，并且只能使用 O(1) 的额外空间。

**核心思路：**
- 把数组看成“下标指向 `nums[i]` 的隐式链表”
- `nums[i]` 表示从下标 `i` 下一步要跳到的下标
- 因为数字范围是 `1 ~ n`，不会跳回下标 `0`，所以 `0` 只是进入链表的起点
- 重复数字表示有两个不同下标指向同一个下标，这个位置就是环入口
- 用快慢指针先找环内相遇点，再从起点和相遇点同时出发找环入口

**代码：**
```javascript
var findDuplicate = function(nums) {
    let slow = 0;
    let fast = 0;

    do {
        slow = nums[slow];
        fast = nums[nums[fast]];
    } while (slow !== fast);

    let ptr1 = 0;
    let ptr2 = slow;

    while (ptr1 !== ptr2) {
        ptr1 = nums[ptr1];
        ptr2 = nums[ptr2];
    }

    return ptr1;
};
```

**时间复杂度：** O(n)  
**空间复杂度：** O(1)

**易错点：**
- 第一阶段不能直接写 `while (slow !== fast)`，因为初始时 `slow` 和 `fast` 都是 0，循环不会执行
- `fast = nums[nums[fast]]` 表示快指针走两步：先到 `nums[fast]`，再到 `nums[nums[fast]]`
- 第二阶段两个指针都要每次走一步，不能继续一快一慢
- `ptr2 = slow` 和 `ptr2 = fast` 都可以，因为第一阶段结束时二者已经相遇
- 用 `Set` 可以找到重复数，但额外空间是 O(n)，不符合题目 O(1) 空间要求

**学习心得：**
- 这题最难的是建模：把数组理解成“传送门”，当前位置是下标，数组值是下一步跳到哪里
- 重复数字之所以是环入口，是因为两个位置的值相同，也就是两个下标都指向同一个下标
- 快慢指针第一次相遇后，从起点和相遇点各放一个指针同速前进，会在环入口相遇

### 48. 旋转图像
**难度：** 中等 ⭐⭐⭐  
**完成日期：** 2026-06-15

**题目：** 给定一个 `n x n` 的二维矩阵 `matrix`，将图像顺时针旋转 90 度。要求原地修改矩阵。

**核心思路：**
- 顺时针旋转 90 度可以拆成两步：先沿主对角线转置，再反转每一行
- 转置时交换 `matrix[i][j]` 和 `matrix[j][i]`
- 只遍历 `j = i + 1` 到 `n - 1`，避免同一对元素交换两次
- `matrix[i].reverse()` 会把第 `i` 行原地反转

**代码：**
```javascript
var rotate = function(matrix) {
    const n = matrix.length;

    for (let i = 0; i < n; i++) {
        for (let j = i + 1; j < n; j++) {
            const temp = matrix[i][j];
            matrix[i][j] = matrix[j][i];
            matrix[j][i] = temp;
        }
    }

    for (let i = 0; i < n; i++) {
        matrix[i].reverse();
    }
};
```

**时间复杂度：** O(n²)  
**空间复杂度：** O(1)

**易错点：**
- LeetCode 要求原地修改，通常不需要 `return matrix`
- 转置时内层循环应从 `j = i + 1` 开始，否则会重复交换
- `reverse()` 是数组内置方法，会直接修改原数组
- 空间复杂度看的是同一时刻额外占用的空间，不是临时变量执行了多少次

**学习心得：**
- 二维数组中，`matrix[i]` 表示第 `i` 行，`matrix[i][j]` 表示第 `i` 行第 `j` 列
- 顺时针旋转公式是 0-based 下标下的 `[i][j] -> [j][n - 1 - i]`
- 顺时针旋转 90 度 = 先转置，再反转每一行
- 逆时针旋转 90 度也可以用类似思路：先转置，再反转每一列；或者先反转每一行，再转置

---

## 滑动窗口专题

### 3. 无重复字符的最长子串
**难度：** 中等 ⭐⭐⭐  
**完成日期：** 2026-06-16

**题目：** 给定一个字符串 `s`，请找出其中不含有重复字符的最长子串的长度。

**核心思路：**
- 用滑动窗口维护一个“不含重复字符”的连续子串
- 右指针负责探索新字符
- 如果右边遇到窗口里已有的字符，左指针负责收缩窗口
- 窗口重新合法后，更新最大长度

**Set 写法代码：**
```javascript
var lengthOfLongestSubstring = function(s) {
    const set = new Set();
    let left = 0;
    let maxLen = 0;

    for (let right = 0; right < s.length; right++) {
        while (set.has(s[right])) {
            set.delete(s[left]);
            left++;
        }

        set.add(s[right]);
        maxLen = Math.max(maxLen, right - left + 1);
    }

    return maxLen;
};
```

**Map 写法代码：**
```javascript
var lengthOfLongestSubstring = function(s) {
    const map = new Map();
    let left = 0;
    let maxLen = 0;

    for (let right = 0; right < s.length; right++) {
        const char = s[right];

        if (map.has(char) && map.get(char) >= left) {
            left = map.get(char) + 1;
        }

        map.set(char, right);
        maxLen = Math.max(maxLen, right - left + 1);
    }

    return maxLen;
};
```

**时间复杂度：** O(n)  
**空间复杂度：** O(min(n, 字符集大小))

**易错点：**
- Set 写法必须用 `while` 收缩窗口，不能只用 `if` 删除一次；例如 `"abba"` 扫到第二个 `b` 时要删掉 `a` 和第一个 `b`
- Map 写法必须判断 `map.get(char) >= left`，否则会把已经不在窗口里的旧字符也当成重复字符
- `left` 只能向右移动，不能因为历史位置而回退
- 文件中如果写两个同名函数，后面的函数会覆盖前面的函数

**学习心得：**
- 右指针负责探索新字符，左指针负责修复窗口
- 右边遇到同样的字符，左指针要移到之前出现过的那个字符的下一个位置
- 滑动窗口必须始终表示原字符串中的连续区间，不能“丢掉右边字符”来跳过重复

---

*本文档会随着学习进度持续更新*
